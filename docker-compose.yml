version: '3'

services:
  iptv-web-client:
    build: .
    ports:
      - "12000:12000"
    volumes:
      - ./channels.json:/app/channels.json
      - ./uploads:/app/uploads
    restart: unless-stopped
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
      - PORT=12000